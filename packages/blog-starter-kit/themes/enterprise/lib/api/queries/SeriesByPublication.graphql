query SeriesByPublication($host: String!, $first: Int!, $after: String) {
  publication(host: $host) {
	...Publication
    seriesList(first: $first, after: $after){
      edges {
        cursor
        node {
			    ...Series
        }
      }
    }
  }
}